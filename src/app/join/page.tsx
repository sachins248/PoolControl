import '../auth/auth.css'
import { JoinClient } from './join-client'

export const metadata = { title: 'Join Your Facility — PoolControl.ai' }

export default function JoinPage() {
  return <JoinClient />
}
